<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ualberta.cmput301w16t16.glamorousborrowingwhaleapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".IncomingBidsActivity" />
        <activity android:name=".MyItemActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".MyBorrowedItemsActivity" />
        <activity android:name=".MyBidsActivity" />
        <activity android:name=".UserController" />
        <activity android:name=".ProfileViewActivity" />
        <activity
            android:name=".MyItemsActivity"
            android:label="@string/title_activity_my_items"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewListingActivity"
            android:label="@string/title_activity_new_listing"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyBorrowedItemActivity"
            android:label="@string/title_activity_my_borrowed_item"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
